# BBSI inbound SFTP — runbook

**Created:** 2026-08-10 · **Status:** account live but INERT (no key installed — nobody can authenticate)
**Purpose:** BBSI (PEO) delivers a recurring report to Splashworks over SFTP. Inbound only.

## Design decisions

| Decision | Choice | Why |
|---|---|---|
| Account model | **One org account `sftp-bbsi`, one public key per individual** | Individual credentials, individually revocable (delete one line). sshd logs the key fingerprint, so an upload is attributable to a person. One jail = one predictable destination for the report. |
| Direction | **Inbound only — no `extracts/` directory** | We publish nothing to BBSI. An empty `extracts/` reads as "a place to publish to" and invites a future script to do it. |
| Drop-off permissions | **`0700`** (Greenmill uses 0750) | All SFTP accounts share the `sftponly` primary group, so 0750 grants group-read to every other partner account. Chroot makes that unreachable over SFTP today — but a PII drop-off must not depend on a second control for its confidentiality. |
| Separation from Greenmill | **Separate user, separate jail, separate key file** | No shared path, no shared credential. Greenmill's jails were verified unchanged after creation. |
| Shared partner archive | **EXPLICITLY EXCLUDED** (`ARCHIVE_EXCLUDE=sftp-bbsi`) | Payroll/HR PII must not land in the general-purpose archive other tooling reads. Excluded by name, not by accident — BBSI sends no sidecar manifests today, so it would be skipped anyway; the day they send one it would silently start archiving PII to the wrong place. |

## What exists on prod (2.24.202.170)

```
/srv/sftp/sftp-bbsi/            root:root 0755   (chroot jail root)
└── incoming/                   sftp-bbsi:sftponly 0700   ← the only writable path
/etc/ssh/sftp-keys/sftp-bbsi    root:root 0644, 0 bytes   ← INERT until keys land
```

Account: system user, `/usr/sbin/nologin`, password locked (`passwd -S` → `L`), primary group `sftponly`.
Inherits the shared `Match Group sftponly` sshd block: chroot to `/srv/sftp/%u`, `internal-sftp -l INFO`
(per-file transfer logging), keys read from outside the jail, no password auth, no forwarding/tunnels.

Client sees exactly one directory: `/incoming/`.

## BBSI network details (Eric Whitchurch, BBSI IT — 2026-08-27)

- **BBSI egress IPs: `96.95.77.209` and `32.132.45.26`.** Their firewall is open to us.
- Every key line MUST carry `from="96.95.77.209,32.132.45.26"` — a leaked private key is then
  unusable from anywhere but BBSI's network. Closes the source-IP half of IN-17 for this account.
- **Watch item:** Eric asked "who needs an SFTP client installed on their computer," which implies
  manual uploads from individual workstations and, with "install the private key in the SFTP client,"
  possibly ONE private key copied to several laptops. **Ask for one key PER PERSON.** A shared key is
  neither revocable per person nor attributable. If the report is generated on a schedule it should
  upload from a server with a single service key, not from anyone's laptop.

**Terminology note** — Eric's email reads ambiguously: "share the public key with the client" means
share it with *Splashworks*; "install the private key in the SFTP client" means the *software*. Two
senses of "client" in one sentence. His flow is correct: BBSI keeps the private key, sends us the
public half only.

**What the keypair does and does not do:** it authenticates *who is connecting*. It does NOT encrypt
the session — SSH negotiates that separately via ephemeral Diffie-Hellman, so the channel is encrypted
even under password auth. SFTP therefore gives confidentiality IN FLIGHT ONLY; the file lands as
plaintext on our disk. Encryption at rest remains open (OPEN item 5).

## Activation — when BBSI sends their public key(s)

1. Collect one **public** key per individual (`ssh-ed25519` preferred; never accept a private key).
2. Append to `/etc/ssh/sftp-keys/sftp-bbsi`, **one line per person, `from=`-restricted, comment = their email**:
   ```
   from="96.95.77.209,32.132.45.26" ssh-ed25519 AAAA...  jane.doe@bbsi.com
   from="96.95.77.209,32.132.45.26" ssh-ed25519 AAAA...  bob.smith@bbsi.com
   ```
   The file is pre-staged on prod with this guidance inline; paste real keys beneath it.
3. Verify: `ls -l /etc/ssh/sftp-keys/sftp-bbsi` → `root:root 0644`. No reload needed (`AuthorizedKeysFile` is read per-connection).
4. Have them test: `sftp -i <key> sftp-bbsi@2.24.202.170` → `put testfile` into `/incoming/` → confirm the Slack #alerts login+upload notification fires.

**Connection details to give BBSI:** host `2.24.202.170`, port `22`, user `sftp-bbsi`, key auth only, upload path `/incoming/`.
Host key fingerprint (same server as Greenmill): `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIBgimZGkmH3XWc6EyUKpImeI3QRTg9wdd0CJY91RYiu8`

## Offboarding an individual
Delete their line from `/etc/ssh/sftp-keys/sftp-bbsi`. Effective on next connection; no other change.

## What is already wired automatically
- **Slack #alerts** — `notify-sftp-access.sh` matches `sftp-*`, so BBSI logins and uploads alert with no change.
- **Transfer logging** — `internal-sftp -l INFO` records every open/close with filename and byte count.
- **Nightly report** — `report.py` globs `/srv/sftp/*/incoming`, so BBSI files appear in the ingestion email. They will show as **unverified** (BBSI sends no checksum sidecar) — that is honest, not a fault.

## Direction: CONFIRMED PUSH (Ross, 2026-08-28)

BBSI uploads to us; we do not pull from them. Reasoning, so it is not re-litigated:
- **Credential asymmetry runs the right way.** We hold nothing of BBSI's — they keep their private
  key, we hold a public key that is worthless if stolen. A pull model would put a live credential to a
  payroll provider on our disk.
- **Blast radius points outward.** A BBSI compromise can write into a chrooted 0700 dir that nothing
  ingests automatically. Our compromise, under pull, would reach into BBSI.
- **Their change control is the long pole** and is already done for push (firewall policy + keys).
- **We keep the authoritative transfer log** (`internal-sftp -l INFO`) rather than asking them for it
  during an investigation.

**The cost of push, stated plainly: absence is ambiguous.** No file on a given day is
indistinguishable between "they didn't run it", "their job failed", "they uploaded elsewhere", and
"the report was empty". Silence reads exactly like success. Mitigation is an **expected-arrival
check** — register the feed with a cadence + tolerance and alert on the gap (the ETL-10 /
`feed_registry` pattern). This CANNOT be built until BBSI commits to a schedule.

## OPEN — decisions required before anything consumes this data

1. **Custody.** BBSI files currently live only in the partner-writable jail. That is not a backup (see `feedback_writable_location_is_not_a_backup`). A restricted archive path with its own retention clock is needed — destination, retention period, and who may read it are Ross's calls.
2. **Filenames in email.** The nightly report lists drop-off filenames. Harmless for `BBSI_Payroll_Register_20260810.csv`; a leak if BBSI names files per-employee. Check a real sample before the first report goes out; suppress filenames for this source if needed.
3. **Integrity.** No checksum/manifest convention agreed with BBSI. Worth asking — it is what makes a truncated upload detectable.
4. **Retention/disposal.** PII needs a defined lifetime in the jail. The existing audit-retention pattern (redact @90d, delete @365d) is a starting reference.
5. **Encryption at rest** — unresolved; the VPS disk is not encrypted at rest.
6. **Arrival monitoring — blocked on BBSI.** Need a committed cadence (time + days), a predictable
   dated filename convention, and ideally a checksum/manifest sidecar (same shape Greenmill sends;
   without one a truncated upload is undetectable). Until the cadence is known, a missed delivery is
   invisible.
7. **Ingestion.** Nothing reads these files. Whether this data ever enters the warehouse — and under what access controls — is a separate decision, not implied by delivery.

## ACTIVATED 2026-09-08 — first key installed

Ross supplied `sftp-bbsi.pub`. Installed and verified; the account is now **live**.

| Item | Value |
|---|---|
| Key type | **RSA-2048** (not ed25519 as the template above assumes) |
| Fingerprint | `SHA256:40Z7s8izc/gea0P5JfArXGZpK9ce8T/R/EKj/JbEA+0` |
| Delivered format | **RFC 4716** (`---- BEGIN SSH2 PUBLIC KEY ----`), PuTTYgen default export |
| Original comment | `rsa-key-20260828` |
| Installed comment | `bbsi-UNATTRIBUTED-puttygen-20260828` |
| Key lines in file | 1 (`root:root 0644`, `from=` pinned to both BBSI egress IPs) |

**Format gotcha — the delivered file is NOT an OpenSSH public key.** `ssh-keygen -l -f` rejects it
outright ("is not a public key file"). PuTTYgen's *Save public key* button writes RFC 4716; the
OpenSSH one-liner is only in the *"Public key for pasting into authorized_keys"* box at the top of
the PuTTYgen window. Convert before use — the fingerprint is identical either way, so this is purely
an encoding difference:
```
ssh-keygen -i -f sftp-bbsi.pub > sftp-bbsi-openssh.pub
```
It is also 9 lines / ~477 bytes, which is close enough to an unencrypted ed25519 *private* key to be
worth a second look before assuming the worst. Check the header, not the size.

**⚠ ATTRIBUTION GAP — this is the shared-key risk from the Watch item above, now real.** One key
arrived, carrying a PuTTYgen default comment that names no person. If it is copied to several
laptops we cannot revoke or attribute per person, and the `-l INFO` transfer log will show every
upload as the same identity. Installed anyway so BBSI is not blocked, but the comment is deliberately
loud. **Ask Dave/Eric whose key this is, then rename the comment to that person's email.** If more
than one person will upload, ask for one key each.

**⚠ CLIENT-VERSION RISK — RSA + OpenSSH 9.6.** The server's `pubkeyacceptedalgorithms` offers
`rsa-sha2-512` and `rsa-sha2-256` but **not** `ssh-rsa` (the SHA-1 variant, disabled by default since
OpenSSH 8.8). This RSA key works *only* if their client offers a SHA-2 RSA signature:
- PuTTY / WinSCP: fine on **PuTTY ≥ 0.75** / **WinSCP ≥ 5.19**. Older builds offer only `ssh-rsa` and
  will fail.
- The client-side error is the actively misleading `"Server refused our key"`, which reads as a
  *key* problem and sends people re-sending public keys for hours. It is a *client version* problem.
- Tell them the version requirement up front, or have them generate ed25519 instead.
- Server-side confirmation is in the sshd log: `userauth_pubkey: key type ssh-rsa not in
  PubkeyAcceptedAlgorithms`.

**Verified at activation:** effective config for `sftp-bbsi` resolves to `chrootdirectory
/srv/sftp/%u`, `forcecommand internal-sftp -l INFO`, `passwordauthentication no`,
`allowtcpforwarding no`, `permittunnel no`. Jail `/srv/sftp/sftp-bbsi/incoming` is `0700`
`sftp-bbsi:sftponly`, with no `extracts/` dir. `notify-sftp-access.sh` needs no change — it parses
sshd logs generically rather than from an account list, and runs every 5 minutes.

**`ARCHIVE_EXCLUDE` deployed 2026-09-08.** `archive-incoming.sh` on prod now carries both the manifest-naming fix and the BBSI exclusion (backup: `archive-incoming.sh.bak.20260908T182100Z`). Verified by a live run: `SKIP sftp-bbsi` + Greenmill archived + `29 already present, 0 failed`.

**⚠ CONSEQUENCE — BBSI has no durable copy. Tracked as IN-22, must close before their first real report.** Excluding BBSI from the shared archive is the right boundary, but it leaves their files only in `/srv/sftp/sftp-bbsi/incoming/` — partner-writable, so anyone holding that key can delete our only copy. Note the existing script cannot simply be pointed at a new directory: it archives only payloads whose `*.manifest.json` verifies, and BBSI sends none, so it would archive nothing and still report success. The restricted destination needs its own integrity signal — the `.csv.sha256` sidecar being requested of BBSI is the intended one.


## IN-22 CLOSED 2026-09-16 — restricted archive live

BBSI's first successful upload landed at **14:45 UTC on 2026-09-16**
(`Employee Census [920070] 2026-09-16 09.06.09.271.csv`, 29,153 bytes). The custody gap opened by
`ARCHIVE_EXCLUDE` is now closed.

| | |
|---|---|
| Script | `infrastructure/sftp/archive-restricted.sh` |
| Destination | `/opt/splashworks/data/restricted/sftp-bbsi/` — dirs **0700**, files **0600** |
| Schedule | hourly at **:17** (offset from the 00:30 shared archive) |
| Log | `/opt/splashworks/data/restricted-archive.log` |
| Encryption | **none yet** — see IN-23 |
| Retention | mechanism built, **report-only** — see IN-24 |

**Why a second script rather than retargeting `archive-incoming.sh`.** That script copies only a
payload whose sidecar `*.manifest.json` verifies. BBSI sends none, so pointing it at a new directory
would have archived **nothing and exited 0** — a success report for a job that did nothing, which is
the worst possible failure shape for a backup.

**How completion is decided, and why the receipt matters.** SFTP gives no atomic completion signal.
If the partner supplies `<file>.sha256` (or `.sha512`/`.md5`) it is used and the receipt records
`verified: true`. Otherwise the file must be `SETTLE_MIN` (default 10) minutes old **and** unchanged
in size across two reads, and the receipt records `verified: false, verification_method:
"settle-time"`. Every archived file gets a `<file>.receipt.json` carrying sha256, byte count, source
mtime, archive time and that verification state — so a heuristic copy can never later be mistaken for
a verified one.

**This is the concrete argument for the `.sha256` ask.** `etl/report.py` already recognises
`.sha256`/`.sha512`/`.md5`, and so does this script. If BBSI adds one line to their upload step, both
the nightly report and the restricted archive flip from unverified to verified **with no code change
on our side**.

**A bug worth recording, because it will recur.** The first implementation checked idempotency with
`find -name "$name"`. BBSI's filenames contain square brackets — `Employee Census [920070] …` — and
`-name` takes a **glob**, so `[920070]` was parsed as a character class and never matched the real
file. Every run re-archived it as a fresh `.conflict` copy. Fixed by making the check
content-addressed and name-blind (hash the directory, look for these bytes). **Assume partner
filenames are hostile to globs**; this is the same hazard behind the `BBSI_<Report>_YYYYMMDD.csv`
naming request.

**Still open:** IN-23 (encryption at rest — blocked on key management, not code), IN-24 (retention
policy, needs BBSI as PEO), and the filename convention + `.sha256` sidecar request to Dave/Eric.


## Onward distribution to Greenmill — AUTHORIZED 2026-09-16

**Decision (Ross, 2026-09-16), recorded here as the audit trail:** Greenmill / Sam, as the
investors and owners, are authorized to receive **any and all PII and customer confidential
information**. The BBSI employee census is republished to `sftp-greenmill-ci` on that basis so
Sam can ingest it into the Pool Deck Warehouse.

**What the census actually contains** (verified 2026-09-16, all 221 rows):

| Column | State |
|---|---|
| `SSN` | **masked to last four** — `###-##-9999`, zero rows carry 9+ digits |
| `BBSI ID` | `A99999` — **the employee/clock number**, the crosswalk key absent from Skimmer |
| `DOB` | full date of birth, unmasked |
| `Address Line 1/2`, `City`, `State`, `Zip Code` | full home address, unmasked |
| `Gender Code`, `Last Hire Date`, `EE Status Code` | unmasked |

An earlier reading of this file as carrying full SSNs was wrong; it does not.

**Mechanism — `infrastructure/sftp/publish-restricted-to-sftp.sh`, cron `:37` hourly.**
A plain `cp` into `extracts/` was rejected, for reasons unrelated to sensitivity:

1. It bypasses the **fail-closed entitlement check** that exists so a partner can never receive
   data nobody authorized.
2. It leaves **no manifest**, so the recipient cannot tell a complete file from a truncated one —
   the problem `MANIFEST.txt` already solves for the nightly extracts.
3. **Nothing would ever remove it.** `publish-extracts-to-sftp.sh` only cleans up files matching
   `COMPANY_MAP`, so a hand-dropped census would sit in Greenmill's pull folder in perpetuity. A
   one-off share silently becomes a permanent publication.

The script instead publishes **only from the restricted archive**, never the partner-writable
jail — so BBSI cannot cause a publication by uploading. It reads each `*.receipt.json` as the
authority on what we hold, writes `RESTRICTED_MANIFEST.txt` (file, sha256, bytes, received time,
and the **verified** flag so Sam can see which copies are checksum-verified and which rest on the
settle-time heuristic), refuses to republish gpg-encrypted artifacts the recipient has no key for,
and **withdraws** anything no longer present in the restricted archive so retention propagates
instead of leaving Greenmill's copy as the surviving one.

**Cron chain:** `:17` capture to the restricted archive → `:37` republish to entitled accounts.

**Entitlements are explicit pairs and fail closed** — `sftp-greenmill-ci | sftp-bbsi`. Adding a
line there is an authorization decision. Do not replace it with a glob.

**First publication:** 2026-09-16 15:14 UTC, `Employee Census [920070] 2026-09-16 09.06.09.271.csv`,
29,153 bytes, sha256 `f443f3c8…` — byte-identical to the restricted archive copy. The three nightly
Skimmer extracts in that jail were untouched.
